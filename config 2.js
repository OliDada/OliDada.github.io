// Production config - API key will be set by GitHub Actions
// For local development, add your API key here temporarily
window.CONFIG = {
  GEMINI_API_KEY: '' // Add your key here for local testing, but don't commit it
};
